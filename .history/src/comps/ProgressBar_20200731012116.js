import React, {  } from 'react'
import useStorage from './hooks/useStorage'

const ProgressBar = () => {
    render() {
        return (
            <div>
                <useStorage />
            </div>
        )
    }
}

export default ProgressBar
