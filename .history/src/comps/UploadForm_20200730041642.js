import React  from 'react'

const UploadForm = () => {
   const changeHandler
        return (
            <form>
                <input type="file" onChange={changeHandler} />
            </form>
        )
    }
export default UploadForm